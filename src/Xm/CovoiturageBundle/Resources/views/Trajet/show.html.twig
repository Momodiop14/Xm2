{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Trajet</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Prixtrajet</th>
                <td>{{ entity.prixTrajet }}</td>
            </tr>
            <tr>
                <th>Localitedepart</th>
                <td>{{ entity.localiteDepart }}</td>
            </tr>
            <tr>
                <th>Localitedestination</th>
                <td>{{ entity.localiteDestination }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('trajet') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('trajet_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
